# Firebase Configuration for Frontend (Web App)
# Get these values from your Firebase Console:
# 1. Go to https://console.firebase.google.com
# 2. Select your project
# 3. Go to Project Settings (gear icon)
# 4. Scroll down to "Your apps" section
# 5. Select or create a Web App
# 6. Copy the config values from the Firebase SDK snippet

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# For scripts (monthly export) - Server-side only
# These should match the values from GitHub Secrets used in workflows
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Note: The frontend requires NEXT_PUBLIC_* variables to connect to Firestore
# The workflows use FIREBASE_SERVICE_ACCOUNT and FIREBASE_PROJECT_ID from GitHub Secrets
# Make sure both are configured for full functionality
